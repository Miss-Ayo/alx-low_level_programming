/**
 * mul - multiplies two integers.
 * @a: first integers
 * @b: second integers
 * Return: 0
 */

#include "main.h"

int mul(int a, int b)
{
if (a == 0 || b == 0)
return (0);
else
return (a * b);
}
